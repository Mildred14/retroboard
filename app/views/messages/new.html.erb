<%= turbo_frame_tag "new_message", target: "_top" do %>
  <p>Hola from New</p>
  <%= form_with model: [@board, @message] do |f| %>
    <div class="field">
      <%= f.text_area :content, placeholder: 'new message' %>
    </div>

    <div class="field">
      <%= f.select :category, [['Good', 'good'], ['Improve', 'improve'], ['Actions', 'actions']], include_blank: false %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
<% end %>
